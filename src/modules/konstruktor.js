
const konstruktor = () => {

        const onOff = document.getElementById('myonoffswitch'),
            panelGroup = document.getElementById('accordion'),
           headPanels = document.querySelectorAll('.panel-heading');

        // TEXTPANELS
        const textPanels = document.querySelectorAll('.collapse');
        const collapseTwo = document.getElementById('collapseTwo');
        const collapseThree = document.getElementById('collapseThree');
        const collapseVour = document.getElementById('collapseVour');


            };
                export default konstruktor;


                // const toggleTabContent = (index) => {
                //     for (let i = 0; i < panelText.length; i++) {
                //         if (index === i) {
                //             panelText[i].style.display = 'block';
                //         } else {
                //             panelText[i].style.display = 'none';
                //         }
                //     }
                // };

                // accordionGroup.addEventListener('click', (event) => {
                //     event.preventDefault();

                //     let target = event.target;
                //     target = target.closest('.panel-heading');

                //     if (target.classList.contains('panel-heading')) {
                //         panelHead.forEach((item, i) => {
                //             if (item === target) {
                //                 toggleTabContent(i);
                //             }
                //         });
                //     }